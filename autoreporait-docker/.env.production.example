# AutoReportAI 生产环境配置示例
# 复制此文件为 .env.production 并根据您的服务器环境修改

# 项目名称
COMPOSE_PROJECT_NAME=autoreport

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=autoreport
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PORT=6379

# Backend Configuration
BACKEND_PORT=8000
API_WORKERS=2
ENVIRONMENT=production
DEBUG=false
SECRET_KEY=your-very-secure-secret-key-here
ENCRYPTION_KEY=your-encryption-key-here

# CORS Configuration - 生产环境配置
# 方式1: 明确指定允许的来源（推荐）
CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com,http://your-server-ip:3000

# 方式2: 使用正则表达式（更灵活）
# CORS_ORIGIN_REGEX=^https?://(your-domain\.com|www\.your-domain\.com|your-server-ip:3000)$

# 方式3: 本地网络环境（适合内网部署）
# CORS_ORIGINS=http://192.168.1.100:3000,http://10.0.0.100:3000

# 方式4: 开发环境临时方案（不安全，仅用于测试）
# CORS_ORIGINS=*

# CORS 其他设置
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,OPTIONS,PATCH
CORS_ALLOW_HEADERS=*

# AI Provider Configuration
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_DEFAULT_MODEL=gpt-4o-mini

# API Configuration
API_BASE_URL=https://your-domain.com:8000  # 或者 http://your-server-ip:8000
API_RATE_LIMIT=1000

# Frontend Configuration
FRONTEND_PORT=3000
NEXT_PUBLIC_API_URL=https://your-domain.com:8000/api/v1  # 或者 http://your-server-ip:8000/api/v1
NEXT_PUBLIC_WS_URL=wss://your-domain.com:8000/ws  # 或者 ws://your-server-ip:8000/ws
NEXT_PUBLIC_APP_NAME=AutoReportAI
NEXT_PUBLIC_APP_VERSION=1.0.0

# File Storage Configuration
MAX_FILE_SIZE=104857600  # 100MB
FORCE_LOCAL_STORAGE=true

# Email Configuration（生产环境建议启用）
SMTP_SERVER=smtp.your-email-provider.com
SMTP_PORT=587
SMTP_USERNAME=your-email@domain.com
SMTP_PASSWORD=your-email-password
ENABLE_EMAIL_NOTIFICATIONS=true

# Monitoring Configuration
ENABLE_MONITORING=true
LOG_LEVEL=INFO

# 时区配置
TIMEZONE=Asia/Shanghai

# 安全增强配置
CORS_ALLOW_CREDENTIALS=true
# 如果使用HTTPS，建议设置安全头
# SECURE_HEADERS=true